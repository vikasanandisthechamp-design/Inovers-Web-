default_platform(:android)

platform :android do
  desc "Upload pre-built AAB to Play Store internal testing"
  # Note: the CI workflow already builds the AAB before calling this lane.
  # This lane only handles the Play Console upload, not the build.
  lane :beta do
    # Upload to Play Store internal testing
    upload_to_play_store(
      track: "internal",
      aab: "../build/app/outputs/bundle/release/app-release.aab",
      skip_upload_metadata: true,
      skip_upload_changelogs: true,
      skip_upload_images: true,
      skip_upload_screenshots: true,
    )
  end

  desc "Build release APK"
  lane :build_release do
    sh("cd ../.. && flutter build apk --release --dart-define=API_BASE_URL=https://sportgod-backend-production.up.railway.app")
  end

  desc "Build release AAB"
  lane :build_aab do
    sh("cd ../.. && flutter build appbundle --release --dart-define=API_BASE_URL=https://sportgod-backend-production.up.railway.app")
  end
end
