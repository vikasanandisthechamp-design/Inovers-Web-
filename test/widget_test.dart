import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder — Supabase init requires async setup
    expect(1 + 1, equals(2));
  });
}
